# 安全政策

## 支持范围

当前项目属于学习和原型阶段，不提供医疗安全保证，也不应被用于诊断、治疗或紧急救援。

## 报告安全问题

发现凭据泄露、越界访问、BLE 隐私问题或其他安全漏洞时，请不要在公开 Issue 中粘贴敏感信息。请通过 GitHub 仓库维护者资料中提供的私密联系方式报告，并包含：

- 受影响的版本或提交
- 最小复现步骤
- 可能的影响
- 建议修复方式（如有）

## 凭据与健康数据

- Wi-Fi 配置应通过 `idf.py menuconfig` 写入本地 `sdkconfig`。
- `sdkconfig` 已加入 `.gitignore`，不得强制提交。
- 不要记录或上传可识别个人身份的健康数据。
- 如果凭据曾进入 Git 历史，必须先轮换凭据，再考虑公开仓库。
